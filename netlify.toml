# netlify.toml (racine du repo)
[build]
  base = "apps/web"
  command = "npm install --no-audit --no-fund && npm run build"
  publish = ".next"   # IMPORTANT: relatif à base => .next

[build.environment]
  NODE_VERSION = "20"

[[plugins]]
  package = "@netlify/plugin-nextjs"
